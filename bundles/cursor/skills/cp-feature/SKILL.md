---
name: cp-feature
description: "Run the full WordPress feature lifecycle — triage, context, spec, plan, build, test, security, review"
disable-model-invocation: true
---

<!-- Generated by wp-custom-agent-skills scripts/build-bundles.mjs from commands/cp-feature.md. Do not edit here; edit the source and rebuild. -->

# /cp-feature

The complete lifecycle for a new feature: `$ARGUMENTS`

Run these in order, stopping at each gate for confirmation. Do not skip ahead to
implementation — that is the failure this command exists to prevent.

```
/cp-triage    → task type + workflow depth
     ↓
/cp-context   → Plugin Context Record (identity, surface, storage, toolchain)
     ↓
/cp-spec      → surface, data model, capabilities, public API, i18n, acceptance criteria
     ↓          [GATE: user confirms the spec]
/cp-plan      → ordered tasks, WordPress sequencing rules applied
     ↓          [GATE: user approves the plan]
/cp-build     → implement task by task, test-first, guardrails at write time
     ↓
/cp-test      → happy path, no-permission, empty state, bad input, upgrade
     ↓
/cp-security  → enumerate entry points, five questions each
     ↓
/cp-review    → seven axes, including public API/back-compat and i18n
```

Skill loading is progressive: load the skill for the phase you are in, not all of them.
Announce which phase you are entering and what it produced.

If triage returns **Direct** depth, say so and collapse to
`/cp-context → /cp-build → /cp-review` rather than running the full chain — forcing the
whole lifecycle onto a typo is how teams abandon a process.

Escalation is always allowed and always announced; de-escalation is not.
